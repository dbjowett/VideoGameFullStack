const useReleased = (date) => {
  console.log(date);
};

export default useReleased;
// const releaseDate = (function () {
//     if (diffInDays === 0) {
//       return 'Today';
//     } else if (diffInDays === 1) {
//       return `${diffInDays} Day`;
//     } else if (diffInDays < 8) {
//       return `${diffInDays} Days`;
//     } else {
//       return `Err`;
//     }
